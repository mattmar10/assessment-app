'use strict';

describe('myApp.assessment module', function() {

  beforeEach(module('myApp.assessment'));

  describe('assessment controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var assessmentCtrl = $controller('AssessmentCtrl');
      expect(assessmentCtrl).toBeDefined();
    }));

  });
});